// Check if I need to download the storage info below, or just input it here 
// Also, I need to update the code from HikingRVerLife to JournalStockTrades drive...?? 



/* import { defineStorage } from "@aws-amplify/backend";

export const storage = defineStorage({
  name: "amplifyHikingRVerLifeDrive",
  access: (allow) => ({
    "media/{entity_id}/*": [
      allow.entity("identity").to(["read", "write", "delete"]),
    ],
  }),
}); */